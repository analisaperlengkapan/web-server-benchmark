version       = "1.0.0"
author        = "Benchmark"
description   = "HTTP Server Benchmark"
license       = "Apache-2.0"

bin           = @["server"]

requires "nim >= 2.0.0"
requires "jester >= 0.6.0"
